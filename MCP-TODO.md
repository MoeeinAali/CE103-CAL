# کمبودهای Logisim MCP — فهرست کارهای بعدی

هرچه در این فایل آمده، در جریان ساخت Lab01 تا Lab03 عملا به آن برخوردیم و
هرکدام با آزمایش روی همین ماشین تأیید شده است — حدس و آرزو نیست.

مخزن سرور: `/Users/moeein/Desktop/logisim-mcp` — ارجاع‌های `file:line` به همان مخزن است.

**فهرست:**
[P0 باگ‌های تأییدشده](#p0--باگهای-تأییدشده) ·
[P1 قابلیت‌های گمشده](#p1--قابلیتهای-گمشده) ·
[P2 کیفیت کار](#p2--کیفیت-کار) ·
[P3 بلندمدت](#p3--بلندمدت) ·
[چیزهایی که سالم‌اند](#چیزهایی-که-سالماند)

---

## P0 — باگ‌های تأییدشده

اینها باگ‌اند نه کمبود؛ کم‌هزینه و پرسودند.

### ۱. پین خروجی به‌عنوان ورودی گزارش می‌شود

`describe_circuit` صفت `output="true"` را می‌خواند، ولی Logisim (و خود writer همین
سرور) خروجی را به شکل `<a name="type" val="output"/>` می‌نویسد. نتیجه: **هر پین
خروجی در هر مداری `IN` گزارش می‌شود** — حتی در مدارهایی که خود MCP ساخته.

شواهد — روی `Lab01/bcd_adder.circ` که کاملا با MCP ساخته شده:

```
Pins:
  IN   A     @ (100,100)  1-bit
  IN   Sum   @ (640,110)  1-bit    ← خروجی است
  IN   Cout  @ (650,260)  1-bit    ← خروجی است
```

اثر جانبی: در بخش `Nets` هم جهت پورت غلط درمی‌آید (`Sum.tip ->` یعنی راننده)،
پس یک نت با دو راننده‌ی ظاهری نشان داده می‌شود و شبیه اتصال کوتاه به‌نظر می‌رسد.

سه نقطه‌ی مشترک:
`tools/describe_tools.py:41`، `tools/describe_tools.py:167`، `tools/builder_tools.py:47`

اصلاح: `comp.attrs.get("type") == "output" or comp.attrs.get("output") == "true"`.

### ۲. نت‌های متصل به قطعات ناشناخته نامرئی‌اند

`_spec_for` (`tools/describe_tools.py:27`) برای هر قطعه‌ای که مدل پورت ندارد
`None` برمی‌گرداند و کد با `continue` از آن رد می‌شود
(`describe_tools.py:121, 209, 217`). یعنی پورت‌های `Splitter`، `Text`، نمونه‌های
ساب‌مدار و تمام تراشه‌های TTL در هیچ نتی شرکت نمی‌کنند.

شواهد: `describe_circuit` روی `multiplier` (۱۱۸ قطعه، ۹۶ سیم) بخش `Nets` را
**کاملا خالی** چاپ کرد، درحالی‌که مدار سالم است و ۲۷۲۰ گام تست را پاس می‌کند.

اصلاح حداقلی: برای قطعه‌ی ناشناخته دست‌کم `loc` را به‌عنوان یک پورت بی‌نام ثبت
کن و در گزارش با `?` علامت بزن، تا نت‌ها ناپدید نشوند.

---

## P1 — قابلیت‌های گمشده

اینها چیزهایی‌اند که در Lab03 مجبور شدیم دور بزنیم و XML را دستی بنویسیم.

### ۳. کتابخانه‌ی TTL (سری ۷۴)

`SPEC_FACTORIES` (`circ/library.py:498`) فقط گیت‌ها، مالتی‌پلکسر/دمالتی‌پلکسر،
دیکدر، Adder، Comparator، Register، Counter، Constant، Clock، Tunnel و پین دارد.
هیچ تراشه‌ی ۷۴ ندارد، درحالی‌که Logisim حدود ۵۰ تراشه دارد
(`7400 7402 7404 7408 7410 7411 7413 7414 74125 74138 74139 74151 74153 74157
74158 74161 74163 74164 74165 74166 74175 7418 74181 74182 7419 74192 74193
74194 7420 7421 7424 74240 74241 74244 74245 74266 7427 74273 74283 74299 7430
7432 7434 7436 74377 74381 7474 7485 …`).

آزمایشگاه معماری تقریبا همیشه «با تراشه‌ی ۷۴ بساز» می‌خواهد، پس این مهم‌ترین
قابلیت گمشده است.

**فرمول لازم، از سورس Logisim v4.1.0 (`std/ttl/AbstractTtlGate.java`) استخراج و
در Lab03 روی سخت‌افزار واقعی تأیید شده — پس پیاده‌سازی‌اش چند خط است:**

```python
# ارتفاع پیش‌فرض 60 است؛ کران‌های نچرخیده (0, -30, pin_count*10, 60)
def ttl_pin(x, y, pin_count, p, facing="south"):
    n, i = pin_count, p - 1
    if facing == "east":            # DIP افقی
        w = n * 10
        if i < n // 2:
            return (x + i * 20 + 10, y + 30)          # ردیف پایین، چپ به راست
        return (x + w - (i - n // 2) * 20 - 10, y - 30)  # ردیف بالا، راست به چپ
    if facing == "south":           # DIP عمودی
        h = n * 10
        if i < n // 2:
            return (x - 30, y + i * 20 + 10)          # ستون چپ، بالا به پایین
        return (x + 30, y + h - (i - n // 2) * 20 - 10)  # ستون راست، پایین به بالا
```

نکات لازم برای پیاده‌سازی:

- پین `pin_count/2` همان GND و پین `pin_count` همان VCC است؛ وقتی صفت
  `VccGndPorts` خاموش باشد (پیش‌فرض) این دو پورت ندارند، ولی **بقیه‌ی پین‌ها
  مختصات فیزیکی خودشان را نگه می‌دارند** — یعنی نگاشت شماره‌ی پین به مختصات
  مستقل از این صفت است.
- XML: `<lib desc="#TTL" name="7"/>` و
  `<comp lib="7" loc="(x,y)" name="74283"><a name="facing" val="south"/><a name="label" val="U2"/></comp>`
- برای هر تراشه فقط یک جدول «شماره‌ی پین → نام سیگنال» لازم است؛ دقیقا همان
  چیزی که در دیتاشیت هست. Logisim خودش این جدول را در آرایه‌ی `portNames` دارد.
- **تله:** در `74194` لاجسیم `QA` پرارزش‌ترین بیت است و `QD` کم‌ارزش‌ترین
  (شیفت راست از `QA` وارد می‌شود)، ولی در `74161` برعکس `QA` بیت صفر است.
  جدول هر تراشه باید جداگانه از سورس خوانده شود، نه با قاعده‌ی کلی.
- `facing="south"` (تراشه‌ی عمودی) برای سیم‌کشی خیلی بهتر جواب می‌دهد: پین‌ها
  روی دو لبه‌ی چپ/راست می‌افتند و هر پین با یک استاب افقی به تونل می‌رسد.

### ۴. اسپلیتر

`add_component` هیچ `type_key` برای `Splitter` ندارد، درحالی‌که هر مدار
باس‌داری به آن نیاز دارد (Lab01، Lab02 و Lab03 هر سه دستی نوشتندش).

فرمول از `circuit/SplitterParameters.java`:

```python
def splitter_end(x, y, i, fanout, facing, appear, spacing):
    gap, width = spacing * 10, 20
    justify = {"center": 0, "legacy": 0, "right": 1, "left": -1}[appear]
    m = -1 if facing == "west" else 1          # افقی؛ عمودی فرمول جدا دارد
    dy0 = 10 if m * justify > 0 else -(10 + gap * (fanout - 1))
    if justify == 0:
        dy0 = -gap * (fanout // 2)
    return (x + m * width, y + dy0 + gap * i)
```

سر مشترک (combined end) روی خود `loc` است. صفت‌ها: `facing` (پیش‌فرض `east`)،
`fanout` (پیش‌فرض ۲)، `incoming` (پهنای باس)، `appear` (پیش‌فرض `left`)،
`spacing` (۱ تا ۹). با `spacing="3"` فاصله‌ی ۳۰ واحدی می‌دهد که وصل‌کردن به
تونل‌ها را راحت می‌کند.

### ۵. تست مدار ترتیبی

`runner/SeqTester.java` در کامنت اولش خودش را «EXPERIMENTAL — partially working»
معرفی می‌کند و می‌گوید روی `Register`/`Counter` لبه‌ها را مطمئن نمی‌بیند.

**اما لازم نیست:** خود Logisim از قبل مدار ترتیبی را کامل تست می‌کند و ما در
Lab03 با آن ۲۷۲۰ گام را پاس کردیم. فقط دو چیز در پارسر بردار تست هست که سرور
از آن خبر ندارد (`data/TestVector.java` + `circuit/TestVectorEvaluator.java`):

- هدر می‌تواند ستون‌های `<Set>` و `<Seq>` داشته باشد. Logisim در شروع هر `<Set>`
  مدار را ریست می‌کند و بین گام‌های `<Seq>` **وضعیت را نگه می‌دارد**.
- علامت don't-care `<DC>` است (نه `x`؛ `x` خطای پارس می‌دهد) و `<FLOAT>` هم
  برای «باید شناور باشد» وجود دارد.
- در هر ردیف غیر ریست، `Propagator.toggleClocks()` صدا زده می‌شود؛ پس اگر مدار
  قطعه‌ی `Clock` داشته باشد **خود Logisim آن را تیک می‌زند** و ستون `clk` لازم نیست.

```text
<Set> <Seq> A[4] B[4] start clk C[8] end
1 1 0x3 0x3 1 0 <DC> <DC>
1 2 0x3 0x3 1 1 <DC> 0
...
1 10 0x3 0x3 0 1 0x09 1
```

کار پیشنهادی: `SeqTester.java` حذف شود و به‌جایش یک ابزار
`run_sequence(path, circuit, steps)` بیاید که همین فرمت را تولید و اجرا کند.
مستندات فعلی `.vec` در `MCP.md` هم فقط فرمت ساده‌ی ترکیبی را توضیح می‌دهد و باید
به‌روز شود.

### ۶. ظاهر سفارشی ساب‌مدار (`<appear>`)

هیچ ابزاری برای ساختن `<appear>` نیست. `MCP.md` این را «سخت» ثبت کرده بود، ولی
در Lab03 معلوم شد کاملا ساده است:

```xml
<a name="appearance" val="custom"/>
<appear>
  <rect fill="#ffffff" height="140" stroke="#000000" stroke-width="2" width="120" x="-60" y="-70"/>
  <circ-anchor facing="east" x="0" y="0"/>
  <circ-port dir="in"  pin="100,200" x="-60" y="-50"/>
  <circ-port dir="out" pin="1830,300" x="60" y="-30"/>
</appear>
```

`pin` همان `loc` قطعه‌ی Pin داخل ساب‌مدار است، و **محل پورت نمونه = `loc` نمونه
+ `(x, y)`**. همین. یک ابزار `set_circuit_appearance(path, circuit, ports, w, h)`
می‌تواند این را خودکار بسازد و مشکل قدیمی «instantiate کردن ساب‌مدار» را ببندد.

### ۷. بررسی هندسی (DRC)

گران‌ترین باگ دو آزمایش گذشته این بود: **دو سیم هم‌راستا که بازه‌شان هم‌پوشانی
دارد به هم وصل می‌شوند** و بین دو نت بی‌ربط اتصال کوتاه می‌سازند — بدون هیچ خطایی،
فقط خروجی `E` در تست. الان هیچ ابزاری این را نمی‌گیرد.

یک ابزار `check_circuit(path, circuit)` که این چهار مورد را برگرداند:

1. هم‌پوشانی collinear بین سیم‌های دو نت متفاوت (نه فقط تطابق نقطه‌ای).
2. نقطه‌ی انتهایی یک سیم که وسط سیم نت دیگری می‌افتد (اتصال T ناخواسته).
3. برخورد جعبه‌ی محیطی قطعات با هم.
4. سیمی که از داخل بدنه‌ی یک قطعه رد می‌شود.

پیاده‌سازی مرجع (~۵۰ خط، با تست تزریق خطای عمدی) اینجاست:
[`Assignments/Lab03-Binary-Multiplier/build_circ.py`](Assignments/Lab03-Binary-Multiplier/build_circ.py) —
توابع `check` و `check_boxes`. برای مورد ۳ و ۴ به جعبه‌ی محیطی قطعات نیاز است؛
برای تونل از `std/wiring/Tunnel.java` درمی‌آید: `bw = max(10, 6·len(label))`،
`bh = 12`، حاشیه‌ی ۳، سپس اجتماع با نقطه‌ی مبدأ — یعنی حدودا ۲۶×۱۸.

---

## P2 — کیفیت کار

### ۸. تغییر قطعه بدون حذف و ساخت دوباره

الان فقط `add_*` و `remove_component` هست. لازم است:

- `move_component(path, circuit, from_loc, to_loc)` — با سیم‌های وصل‌شده
- `set_attribute(path, circuit, loc, name, value)` — مثلا عوض‌کردن
  `facing`, `label`, `width`, `value` بدون بازساختن قطعه
- `set_label(path, circuit, loc, label)`

### ۹. قطعات کتابخانه‌ی I/O و Base

هیچ‌کدام مدل ندارند: `LED`, `Hex Digit Display`, `7-Segment Display`, `Button`,
`DipSwitch`, `Text`. برای مدار دموی گزارش آزمایشگاه لازم‌اند. در Lab03 مجبور
شدیم دمو را فقط با Pin بسازیم و `Text` را دستی بنویسیم.

### ۱۰. ابزار سیم‌کشی هوشمندتر

`connect` مسیر L شکل می‌کشد ولی نمی‌داند مسیرش از روی چه چیزی رد می‌شود.
یک `route(from, to, avoid=...)` که کانال آزاد پیدا کند (یا دست‌کم بعد از کشیدن
با DRC مورد ۷ چک کند) خیلی از خطاهای دستی را حذف می‌کند.

الگوی امنی که در Lab03 جواب داد و ارزش دارد به‌عنوان یک ابزار درآید —
`pin_to_tunnel(chip, pin, label)`: هر پین با یک استاب کوتاه به تونل نام‌دار
می‌رود و طول استاب‌ها یک‌درمیان ۳۰ و ۹۰ است، تا بدنه‌ی تونل‌های هم‌سطح ۴۰ واحد
از هم فاصله بگیرند و هیچ برخوردی پیش نیاید.

### ۱۱. خروجی تصویری

برای گزارش LaTeX هر بار باید دستی از GUI اسکرین‌شات گرفت. Logisim در CLI هیچ
گزینه‌ی خروجی PNG ندارد (چک شد: `--help` فقط `-b -f -n -s -t -w --save` دارد).
یک ابزار `render_circuit(path, circuit) -> png` که از مدل قطعات و سیم‌ها یک SVG
بسازد، هم برای گزارش و هم برای اینکه خود عامل چیدمان را «ببیند» ارزش زیادی دارد.

### ۱۲. مستندسازی `type_key`ها

`add_component` وقتی کلید ناشناخته بگیرد لیست معتبرها را در پیام خطا برمی‌گرداند
(`circ/library.py:523`) که خوب است، ولی هیچ ابزاری نیست که **بدون خطا** این لیست
را بدهد. یک `list_component_types()` که کلیدها را همراه نام پورت‌هایشان برگرداند،
یک رفت‌وبرگشت آزمون‌وخطا را حذف می‌کند.

---

## P3 — بلندمدت

- **هندسه‌ی دقیق مالتی‌پلکسر:** `MCP.md` ثبت کرده که تقریبی است و باید با
  `Bit Extender` دور زده شود. سورس `std/plexers/Multiplexer.java` جواب دقیق را دارد.
- **کتابخانه‌ی Memory:** `RAM`, `ROM`, `D/JK/T Flip-Flop`, `Shift Register` هیچ‌کدام مدل ندارند.
- **کتابخانه‌ی Arithmetic:** فقط `Adder` و `Comparator` هست؛ `Subtractor`,
  `Multiplier`, `Divider`, `Negator`, `Shifter`, `BitAdder` نیست.
- **ورودی/خروجی HDL:** Logisim از VHDL/Verilog خروجی می‌گیرد (`-f, --test-fpga`
  با گزینه‌ی `HDLONLY`)؛ می‌شود به‌عنوان راه دوم راستی‌آزمایی استفاده کرد.
- **کش کردن JVM:** هر فراخوانی تست یک JVM جدید بالا می‌آورد. برای بردار ۲۵۶۰
  گامی چند ده ثانیه طول کشید. یک پروسه‌ی ماندگار Logisim این را خیلی سریع می‌کند.

---

## چیزهایی که سالم‌اند

اینها را بررسی کردیم و مشکلی ندارند — برای اینکه دوباره وقت صرفشان نشود:

- **round-trip فایل بدون تلفات است.** `parser.parse` → `writer.save` روی
  `multiplier.circ` (۶۶۴ خط) دقیقا ۶۶۴ خط برگرداند و `<appear>`, `<toolbar>`,
  `<mappings>`, `<options>`, `<lib desc="#TTL">`, قطعات `Text` و
  `appearance="custom"` همگی سالم ماندند. مدل با `extras_raw_xml` هرچه را
  نمی‌شناسد خام نگه می‌دارد. پس **می‌شود با خیال راحت فایل دست‌ساز را با ابزارهای
  MCP ویرایش کرد.**
- `run_test_vector` خودش کاملا درست کار می‌کند؛ محدودیت در مستندات فرمت بود نه در ابزار.
- `create_circuit_file` هنوز `<mappings/>` خالی می‌سازد (تله‌ی ثبت‌شده در `MCP.md`)
  — این یک باگ کوچک است ولی راه‌حلش در `MCP.md` هست.
